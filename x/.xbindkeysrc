"/usr/bin/slock"
    Control+Alt + l

"pamixer -i 5; pkill -RTMIN+2 goblocks"
    XF86AudioRaiseVolume

"pamixer -d 5; pkill -RTMIN+2 goblocks"
    XF86AudioLowerVolume

"mute=$(pamixer --get-mute); if ($mute); then pamixer -u; else pamixer -m; fi; pkill -RTMIN+2 goblocks"
    XF86AudioMute

"/usr/bin/xbacklight -inc 10"
    XF86MonBrightnessUp

"/usr/bin/xbacklight -dec 10"
    XF86MonBrightnessDown
